# PR #8 obsolete Jammer runtime archive

These files are retained only as non-executable historical provenance for the removed destructive/enemy Broadcast Jammer prototype and replacement snippets. They are outside the active browser script order and are intentionally renamed to `.txt` where they were JavaScript so Makko and repository JS audits cannot execute them.

They are not future-design authority. PR #8 uses `src/game/jammer-environment.js` for the active environmental Jammer owner.
