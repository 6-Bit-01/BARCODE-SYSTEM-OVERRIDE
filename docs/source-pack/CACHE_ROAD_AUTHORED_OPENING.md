# Cache Road — the first road lesson

The owner likes the predictable music choice but wants more direction. This pass gives the existing chase an immediate job: deliver the original recording because the clean copy erased the names. A short prompt sits in the sky above the horizon while Cache drives. There is no pause, choice menu, new input owner or campaign award. The four lane names and five supplied MP3s remain unchanged.

| Road moment | What the player learns | What ends the button prompt |
| --- | --- | --- |
| Start and first freight | Hold a lane for half a second to choose its part for the next bar; E/RB plays that part now and carries it through the next four bars; steer clear of freight. | A real lane choice and successful E/RB seal, respectively. |
| Paired block | The outside lanes form a route. Space/A spends a short protected Turbo burst near the block. | A real Turbo use; the route warning remains through the block. |
| First marker and audit | The marker refills Echo. Hold the far-left gap through the three-wide block, send H/Y Echo near the scan and move away after the block. The audit follows the decoy's lane. | A real Echo use changes the instruction to the split; the actual audit commitment changes it to escape. |

The marker's Echo refill is saved. The first audit and the later original exit give Echo six seconds so the player can see the decoy and split; other Echo uses remain short. The first two road markers now validate as version-4 Continue saves at their actual crossing positions. Opening hints are transient state, reset on retry and Continue, and end after the first stretch. The final exit keeps its existing cue.

## Verification and limits

The production harness drives from the first marker through the left gap, sends Echo, observes the audit target the decoy, and exits that lane without damage. It checks the saved Echo refill, retry/Continue validation, lane hold, E/RB, Turbo and controller prompt binding. Native frames show the prompt in the sky while traffic remains visible. The full song, audio assets, mix rule, damage, old saves and final exit still run through repository checks. Actual instruction timing and driving feel need the owner's merged Makko play with sound and a controller. This opening is a direction pass for the proof, not a completed Level 2 chapter or scene.
