# Cache Road FX bed and lane handoff pass

The owner heard all four real MP3 parts in Makko after merged #98 (`ae1be4a1ee868f0307daa0e8e47f7a100a1510e1`). The problem now is the musical handoff while steering: the old mix selected one stem on the next beat and used the same 0.16-second fade in both directions. Bass, Drums, Harmony and FX have very different weight, so a hard replacement can leave a hole or an abrupt jump.

This interim pass uses those exact four aligned 187.5-second files. FX stays at a quiet bed level even when another lane is selected; lane four raises the same running FX stem instead of scheduling a duplicate. The present FX audio is near silent for several seconds at the opening, so a faint level of the existing Drums stem keeps a pulse when its lane is not selected. The Drums lane still raises it to its full level. Bass comes down slightly, Harmony rises slightly, and these gains remain listening choices for Makko review.

Each selected lane settles briefly, then enters on the next beat. A fast crossing can reach its destination without briefly foregrounding every lane passed. An incoming part fades over 0.28 seconds; a departing part fades over 0.82 seconds, while the FX bed and quiet groove remain. Web Audio gain ramps hold the current gain before retargeting. Locks remain additive. All four sources keep the existing common AudioContext start and native loop; there is no seeking, source restart, new clock or save migration.

The owner is authoring another Harmony/instrumental part. When supplied, it can take lane four while the current FX source stays underneath all four lanes. A separate gang vocal source earned by strong driving and joined at a phrase boundary is also planned. Neither audio asset exists in this pass, so it makes no claim to contain either new sound or to implement the vocal reward. The initial FX bed may benefit from a revised owner export with a continuous subtle pulse; the interim Drums floor uses only the current files.

The focused production check asserts synchronized starts, the FX/drum floors, foreground FX boost, short-crossing suppression and longer departure ramps. Real Chromium decodes the committed MP3s, changes lanes in a running graph and measures nonzero output after the change, including the missing-local-asset recovery path. The owner must merge this pass to listen in Makko. See the current route in `ACCEPTANCE.md`.
